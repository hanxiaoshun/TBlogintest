import wx


def note():
    """"doc
    """
    frame = wx.Frame(None, -1, "测试哦")
    nb = wx.NoteBook(frame)
    # AddPage(self, page, text, select=False, imageId=NO_IMAGE)
    
    p1 = Panel(parent, id=ID_ANY, pos=DefaultPosition, size=DefaultSize,
          style=TAB_TRAVERSAL, name=PanelNameStr)
    wx.Panel
    nb.AddPage(wx.Panel(parent,))
    
    


 if __name__ == "__main__":